version: 'version'

services:
  service_maria:
    image: "image_maria"
    ports:
      - "8080:80"
      - 443:443
  service_angelo:
    image: "image_angelo"
    labels:
      - "dchc.port.80.enable=true"
      - "dchc.port.80.protocol=http"
      - "dchc.port.80.http.hostname=archive.ubuntu.com"
      - "dchc.port.80.http.uri=/"
      - "dchc.port.80.http.method=GET"
      - "dchc.port.80.timeout=3"
      - "dchc.port.80.retries=3"
    ports:
      - 82:80
      - 22:443
  service_joao:
    image: "image_joao"
    labels:
      - "dchc.enable=true"
      - "dchc.protocol=http"
      - "dchc.hostname=archive.ubuntu.com"
      - "dchc.http.endpoint=/"
      - "dchc.http.method=GET"
      - "dchc.timeout=3"
      - "dchc.retries=3"
    ports:
      - 80
      - "23"
      - 9180

volumes:
  volume_maria:
    driver: local
